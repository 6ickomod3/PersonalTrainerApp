// Deprecated: this file is no longer used.
// The floating timer now sizes itself via .safeAreaInset(edge: .bottom) in ContentView,
// so the shared height-publishing TimerState class is unnecessary.
// Remove this file from the Xcode project navigator (right-click → Delete → Move to Trash).
